spring:
  cloud:
    gateway:
      routes:
        - id: project-service
          uri: lb://project-service
          predicates:
            - Path=/projects/**
          filters:
            - StripPrefix=1
